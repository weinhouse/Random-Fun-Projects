// secrets.h.template
// ─────────────────────────────────────────────────────────────────
// Copy this file to `secrets.h` (same directory) and fill in real
// values BEFORE flashing.
//
//   cp secrets.h.template secrets.h
//   # edit secrets.h with real values
//
// secrets.h is gitignored at the repo root (**/secrets.h) — do
// NOT commit it. This is a PUBLIC repo; real credentials in commits
// would leak immediately.
// ─────────────────────────────────────────────────────────────────

#define MQTT_USER     "microcontroller"
#define MQTT_PASSWORD "your-mqtt-microcontroller-password"
#define WIFI_PASSWORD "your-wifi-password"
